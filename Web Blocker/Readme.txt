This Website Blocker works actually for Unix Distributions.
You must set the crontab to use it at reboot whith next commands:
'sudo crontab -e'  and then add this line '@reboot python3  path_to_the_file'

By default this Blocker only blocks 'facebook.com' and 'youtube.com'.
You can add as many pages as you wish to block.
